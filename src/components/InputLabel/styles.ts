import styled from '@emotion/styled';

export const InputLabelSC = styled.label`
    margin-bottom: 0.25rem;
    
    font-weight: 400;
    font-size: 1rem;
    line-height: 1.5;
    letter-spacing: 0.01em;
  
    color: ${(props) => props.theme.palette.grey[70]};
`;
